export const httpTheory = [
  {
    time: '00:00',
    title: 'Введение в HTTP',
    content: `HTTP (HyperText Transfer Protocol) — это протокол прикладного уровня для передачи гипертекста, который является основой всемирной паутины. Разработанный Тимом Бернерсом-Ли в 1989 году, HTTP определяет правила взаимодействия между клиентами (обычно веб-браузерами) и серверами.

Протокол работает по принципу "запрос-ответ": клиент отправляет HTTP-запрос серверу, а сервер возвращает HTTP-ответ. Это stateless протокол, то есть каждый запрос обрабатывается независимо, без сохранения информации о предыдущих запросах.

HTTP используется не только для загрузки веб-страниц, но и для передачи данных между различными сервисами, реализации REST API, загрузки файлов и многого другого. Понимание HTTP критически важно для любого разработчика и DevOps-инженера.`,
  },
  {
    time: '00:59',
    title: 'IP-адреса и сетевая адресация',
    content: `IP-адрес (Internet Protocol address) — это уникальный числовой идентификатор устройства в сети. Он состоит из четырех чисел от 0 до 255, разделенных точками, например: 192.168.1.1. Этот формат называется IPv4.

Каждое устройство, подключенное к интернету, имеет свой IP-адрес. Когда вы вводите адрес веб-сайта в браузере, система DNS (Domain Name System) преобразует доменное имя в IP-адрес, после чего браузер может установить соединение с нужным сервером.

IP-адреса делятся на публичные (доступные из интернета) и частные (используемые в локальных сетях). Механизм NAT (Network Address Translation) позволяет множеству устройств в локальной сети использовать один публичный IP-адрес для выхода в интернет.`,
  },
  {
    time: '04:32',
    title: 'Порты и их назначение',
    content: `Порт — это числовой идентификатор (от 0 до 65535), который позволяет направлять сетевой трафик к конкретному приложению на компьютере. Полный сетевой адрес состоит из IP-адреса и порта, например: 192.168.1.1:8080.

Порты делятся на три диапазона:
- 0-1023: системные порты (требуют прав администратора)
- 1024-49151: зарегистрированные порты (для известных приложений)
- 49152-65535: динамические порты (временные)

Для HTTP используется порт 80 по умолчанию, для HTTPS — порт 443. Это означает, что когда вы открываете http://example.com, браузер автоматически подключается к порту 80 на сервере example.com. При указании другого порта его нужно явно указывать в URL: http://example.com:8080`,
  },
  {
    time: '05:55',
    title: 'Архитектура клиент-сервер',
    content: `Клиент-серверная архитектура — это модель взаимодействия, в которой клиент (например, браузер) запрашивает услуги или ресурсы у сервера, который обрабатывает запросы и возвращает ответы.

Клиент всегда инициирует взаимодействие. Сервер пассивно ожидает входящие запросы и отвечает на них. Один сервер может обслуживать множество клиентов одновременно, используя многопоточность или асинхронную обработку.

Преимущества этой архитектуры:
- Централизованное управление данными и бизнес-логикой
- Упрощенное обновление приложений (только на сервере)
- Разделение ответственности между клиентом и сервером
- Возможность использовать различные типы клиентов (браузер, мобильное приложение, IoT устройство)`,
  },
  {
    time: '08:44',
    title: 'Протоколы и стандартизация',
    content: `Протокол — это набор правил и соглашений для обмена данными между компьютерами. Стандартизация протоколов обеспечивает совместимость различных систем независимо от производителя или платформы.

HTTP является протоколом прикладного уровня (Layer 7) в модели OSI. Он работает поверх TCP/IP и определяет формат сообщений, методы запросов, коды состояния ответов и заголовки.

Стандарты HTTP разрабатываются и поддерживаются организацией IETF (Internet Engineering Task Force). Основные версии протокола:
- HTTP/0.9 (1991) — простейшая версия, только GET запросы
- HTTP/1.0 (1996) — добавлены методы POST, HEAD, заголовки
- HTTP/1.1 (1997) — persistent connections, chunked transfer
- HTTP/2 (2015) — multiplexing, server push, сжатие заголовков
- HTTP/3 (2022) — работа поверх QUIC вместо TCP`,
  },
  {
    time: '09:47',
    title: 'Модель запрос-ответ',
    content: `HTTP использует модель взаимодействия "запрос-ответ" (request-response). Каждое взаимодействие начинается с того, что клиент отправляет HTTP-запрос серверу. Сервер обрабатывает запрос и возвращает HTTP-ответ.

Важные характеристики этой модели:
- Клиент всегда инициирует взаимодействие — сервер не может отправить данные без запроса
- Каждый запрос независим (stateless) — сервер не хранит информацию между запросами
- Синхронность — клиент ожидает ответа после отправки запроса

Для поддержания состояния между запросами используются дополнительные механизмы: cookies, session storage, токены аутентификации. Это позволяет создавать stateful приложения поверх stateless протокола.`,
  },
  {
    time: '11:15',
    title: 'Структура HTTP-запроса',
    content: `HTTP-запрос состоит из нескольких частей:

1. **Стартовая строка** (Request Line): содержит метод, путь и версию HTTP
   Пример: GET /index.html HTTP/1.1

2. **Заголовки** (Headers): мета-информация о запросе
   Пример: Host: example.com, User-Agent: Mozilla/5.0

3. **Пустая строка**: разделяет заголовки и тело

4. **Тело запроса** (Body): опционально, содержит данные (для POST, PUT запросов)

Обязательными являются только стартовая строка и заголовок Host (в HTTP/1.1). Все остальное опционально и зависит от типа запроса и требований приложения.`,
  },
  {
    time: '11:56',
    title: 'Доменные имена и DNS',
    content: `Доменное имя — это человеко-читаемый адрес веб-сайта, например youtube.com. Под капотом это имя преобразуется в IP-адрес с помощью системы DNS (Domain Name System).

Процесс разрешения DNS:
1. Браузер проверяет локальный кэш DNS
2. Запрос отправляется к рекурсивному DNS-серверу (обычно у провайдера)
3. Если адрес не найден в кэше, запрос идет к корневым DNS-серверам
4. Затем к DNS-серверам домена верхнего уровня (.com, .org)
5. Наконец, к авторитативному DNS-серверу конкретного домена
6. IP-адрес возвращается браузеру и кэшируется

Домены имеют иерархическую структуру: subdomain.example.com, где .com — домен верхнего уровня, example — домен второго уровня, subdomain — поддомен.`,
  },
  {
    time: '12:44',
    title: 'URL и маршрутизация',
    content: `URL (Uniform Resource Locator) — это полный адрес ресурса в интернете. Он состоит из нескольких компонентов:

**Структура URL:**
protocol://domain:port/path?query#fragment

Пример: https://example.com:443/api/users?id=123&sort=name#top

- **protocol** (https) — протокол передачи данных
- **domain** (example.com) — доменное имя сервера
- **port** (443) — порт (опционально, по умолчанию 80 для HTTP, 443 для HTTPS)
- **path** (/api/users) — путь к ресурсу на сервере
- **query** (?id=123&sort=name) — параметры запроса
- **fragment** (#top) — якорь для навигации внутри страницы

Путь (path) используется для маршрутизации на сервере — определения, какой код должен обработать запрос. В REST API пути обычно соответствуют ресурсам: /users, /posts, /comments.`,
  },
  {
    time: '15:13',
    title: 'HTTP-методы',
    content: `HTTP-методы (или HTTP-глаголы) указывают тип действия, которое клиент хочет выполнить с ресурсом. Основные методы:

**GET** — получение ресурса. Не должен изменять данные на сервере (idempotent и safe).
Пример: GET /users/123 — получить пользователя с ID 123

**POST** — создание нового ресурса. Данные отправляются в теле запроса.
Пример: POST /users — создать нового пользователя

**PUT** — полное обновление существующего ресурса (idempotent).
Пример: PUT /users/123 — заменить все данные пользователя 123

**PATCH** — частичное обновление ресурса.
Пример: PATCH /users/123 — обновить отдельные поля пользователя

**DELETE** — удаление ресурса (idempotent).
Пример: DELETE /users/123 — удалить пользователя 123

**HEAD** — как GET, но возвращает только заголовки без тела.

**OPTIONS** — получить информацию о доступных методах для ресурса.

Idempotent означает, что повторное выполнение запроса дает тот же результат, что и однократное.`,
  },
  {
    time: '19:08',
    title: 'Тело HTTP-запроса',
    content: `Тело запроса (Request Body) содержит данные, которые клиент отправляет серверу. Оно используется преимущественно в методах POST, PUT и PATCH.

Заголовок Content-Type указывает формат данных в теле:

**application/json** — данные в формате JSON (самый популярный для API):
{"name": "Иван", "age": 25}

**application/x-www-form-urlencoded** — данные HTML-формы:
name=Иван&age=25

**multipart/form-data** — для загрузки файлов с дополнительными полями

**text/plain** — простой текст

**application/xml** — данные в формате XML

Размер тела обычно ограничен настройками сервера (например, 1-10 МБ). Для больших файлов используется chunked transfer encoding или специализированные сервисы хранения.`,
  },
  {
    time: '21:20',
    title: 'Формат JSON',
    content: `JSON (JavaScript Object Notation) — это легковесный текстовый формат обмена данными, основанный на синтаксисе объектов JavaScript. JSON стал де-факто стандартом для веб-API благодаря простоте и универсальности.

Основные типы данных JSON:
- Объект: {"key": "value"}
- Массив: [1, 2, 3]
- Строка: "текст"
- Число: 42 или 3.14
- Boolean: true или false
- null: null

Пример JSON с вложенной структурой:
{
  "id": 123,
  "name": "Иван Петров",
  "email": "ivan@example.com",
  "active": true,
  "roles": ["admin", "user"],
  "profile": {
    "age": 25,
    "city": "Москва"
  }
}

JSON читается и записывается на всех популярных языках программирования. В JavaScript: JSON.parse() для разбора, JSON.stringify() для преобразования в строку.`,
  },
  {
    time: '22:32',
    title: 'HTTP-ответ и коды состояния',
    content: `HTTP-ответ сервера имеет структуру, аналогичную запросу:

1. **Статусная строка**: HTTP/1.1 200 OK
2. **Заголовки**: Content-Type, Content-Length и др.
3. **Пустая строка**
4. **Тело ответа**: HTML, JSON, XML или другие данные

**Коды состояния (Status Codes):**

**1xx — Информационные:**
100 Continue — сервер готов принять тело запроса

**2xx — Успешные:**
200 OK — успешный запрос
201 Created — ресурс создан (обычно после POST)
204 No Content — успех, но нет тела ответа

**3xx — Перенаправление:**
301 Moved Permanently — ресурс перемещен навсегда
302 Found — временное перенаправление
304 Not Modified — ресурс не изменился (кэширование)

**4xx — Ошибки клиента:**
400 Bad Request — некорректный запрос
401 Unauthorized — требуется аутентификация
403 Forbidden — доступ запрещен
404 Not Found — ресурс не найден
429 Too Many Requests — превышен лимит запросов

**5xx — Ошибки сервера:**
500 Internal Server Error — внутренняя ошибка сервера
502 Bad Gateway — ошибка шлюза
503 Service Unavailable — сервис недоступен`,
  },
  {
    time: '23:48',
    title: 'Заголовки HTTP',
    content: `HTTP-заголовки передают дополнительную информацию о запросе или ответе. Каждый заголовок — это пара "ключ: значение".

**Основные заголовки запроса:**
- **Host**: example.com — обязательный в HTTP/1.1
- **User-Agent**: информация о браузере/приложении
- **Accept**: какие типы контента клиент понимает
- **Authorization**: токен для аутентификации
- **Cookie**: cookies для сохранения состояния
- **Content-Type**: тип данных в теле запроса

**Основные заголовки ответа:**
- **Content-Type**: тип возвращаемых данных
- **Content-Length**: размер тела ответа в байтах
- **Set-Cookie**: установка cookie на клиенте
- **Cache-Control**: директивы для кэширования
- **Access-Control-Allow-Origin**: настройки CORS

**Кастомные заголовки:**
Можно создавать собственные заголовки, обычно с префиксом X- (хотя это устарело):
X-Request-ID: уникальный идентификатор запроса
X-API-Key: ключ API для аутентификации

Заголовки чувствительны к регистру только в части значения, имена заголовков регистронезависимы.`,
  },
]

