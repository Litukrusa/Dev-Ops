# Вводный Курс по DevOps

Современное веб-приложение для изучения основ DevOps с интерактивными тестами и подробной теорией.

## 📚 Содержание курса

Курс состоит из 4 модулей, расположенных в рекомендуемом порядке изучения:

1. **HTTP Протокол** - Основы HTTP, клиент-серверное взаимодействие, REST API
2. **TCP/IP** - Сетевая архитектура, протоколы транспортного и межсетевого уровней
3. **Docker под капотом** - Контейнеризация, namespaces, cgroups, изоляция процессов
4. **DevOps Roadmap 2025** - Полный путь развития в DevOps: инструменты, практики, технологии

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для production
npm run build

# Запуск production сборки
npm start
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Развертывание с Docker

#### Использование Docker Compose (рекомендуется)

```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

#### Использование Docker напрямую

```bash
# Сборка образа
docker build -t devops-course .

# Запуск контейнера
docker run -p 3000:3000 devops-course
```

## 🔧 Развертывание на Dokploy

### Шаг 1: Подготовка репозитория

1. Создайте Git репозиторий и загрузите код:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin <your-repo-url>
git push -u origin main
```

### Шаг 2: Настройка в Dokploy

1. Войдите в панель управления Dokploy
2. Создайте новый проект
3. Выберите тип развертывания: **Docker Compose**
4. Подключите ваш Git репозиторий
5. Укажите файл docker-compose.yml
6. Настройте переменные окружения (если требуется)
7. Запустите развертывание

### Шаг 3: Настройка домена

1. В настройках проекта добавьте свой домен
2. Dokploy автоматически настроит SSL сертификат через Let's Encrypt
3. Ваше приложение будет доступно по адресу `https://your-domain.com`

### Альтернатива: Развертывание через Dockerfile

Если вы предпочитаете использовать только Dockerfile:

1. В Dokploy выберите тип развертывания: **Dockerfile**
2. Укажите путь к Dockerfile (корень репозитория)
3. Установите порт: **3000**
4. Запустите развертывание

## 🎨 Технологический стек

- **Frontend**: Next.js 14, React 18, TypeScript
- **Стилизация**: Tailwind CSS
- **Иконки**: Lucide React
- **Развертывание**: Docker, Docker Compose
- **Хостинг**: Dokploy (или любая Docker-совместимая платформа)

## 📖 Структура проекта

```
devops-intro-course/
├── app/                      # Next.js App Router
│   ├── layout.tsx           # Корневой layout
│   ├── page.tsx             # Главная страница
│   ├── globals.css          # Глобальные стили
│   └── module/
│       └── [slug]/
│           └── page.tsx     # Страница модуля
├── data/
│   └── modules.ts           # Данные модулей (теория + тесты)
├── public/                  # Статические файлы
├── Dockerfile              # Docker конфигурация
├── docker-compose.yml      # Docker Compose конфигурация
├── package.json            # Зависимости проекта
└── README.md              # Документация
```

## ✨ Возможности

- 📱 **Адаптивный дизайн** - отлично выглядит на всех устройствах
- 🎯 **Интерактивные тесты** - проверьте свои знания после каждого модуля
- 📚 **Подробная теория** - структурированный материал с временными метками
- 🎨 **Современный UI** - красивый темный интерфейс с градиентами
- ⚡ **Высокая производительность** - оптимизированная сборка Next.js
- 🐳 **Docker-ready** - готов к развертыванию в контейнере

## 🔄 Обновление контента

Чтобы добавить или изменить контент курса, отредактируйте файл `data/modules.ts`:

```typescript
export const modules: Record<string, Module> = {
  'module-slug': {
    slug: 'module-slug',
    title: 'Название модуля',
    videoUrl: 'https://youtube.com/...',
    theory: [
      {
        time: '00:00',
        title: 'Раздел теории',
        content: ['Пункт 1', 'Пункт 2', ...]
      }
    ],
    quiz: [
      {
        id: 1,
        question: 'Вопрос?',
        options: ['Вариант 1', 'Вариант 2', ...],
        correctAnswer: 0,
        explanation: 'Объяснение правильного ответа'
      }
    ]
  }
}
```

## 🐛 Отладка

### Проблемы с запуском

Если приложение не запускается:

```bash
# Очистка кэша Next.js
rm -rf .next

# Переустановка зависимостей
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с Docker

```bash
# Пересборка без кэша
docker-compose build --no-cache

# Просмотр логов
docker-compose logs -f web
```

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад в проект

Если вы хотите добавить новые модули или улучшить существующие:

1. Форкните репозиторий
2. Создайте ветку для ваших изменений
3. Внесите изменения и протестируйте их
4. Создайте Pull Request с описанием изменений

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

- Создайте Issue в репозитории
- Проверьте логи приложения
- Убедитесь, что все зависимости установлены корректно

## 🎓 Для студентов

### Задание по развертыванию

1. Склонируйте этот репозиторий
2. Изучите структуру проекта
3. Разверните приложение локально с помощью Docker
4. Разверните приложение на Dokploy
5. Настройте свой домен
6. Предоставьте ссылку на развернутое приложение

### Критерии оценки

- ✅ Приложение работает и доступно по URL
- ✅ SSL сертификат настроен корректно
- ✅ Все модули отображаются правильно
- ✅ Тесты работают без ошибок
- ✅ Дизайн сохранен (нет поломанных стилей)

---

**Удачи в изучении DevOps! 🚀**

